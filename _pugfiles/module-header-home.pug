.header-home
	.touch-toggle
		img(src="/assets/imgs/icons/ic_menu_black_36px.svg")
	
	a(href="/", id="{% unless page.homepage %}hide-touch{% endunless %}").avatar <!-- adds id on non homepages -->
	
	.header-title(id="{% unless page.homepage %}hide-touch{% endunless %}")
		a(href="/")
			h2
		| {% if page.homepage %} <!-- only build nav if on homepage -->
		.line-graphic
		ul.header-nav
			li
				a(href="/visualization")
					h3 Visualization
					.dot
			li
				a(href="/architecture")
					h3 Architecture
					.dot
			li
				a(href="/furniture")
					h3 Furniture
					.dot
		| {% endif %}
